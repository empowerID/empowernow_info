---
import Base from '../../../layouts/Base.astro';
const title = 'Primer — Idempotent Workflows & Approvals';
const description = 'Design reliable identity operations with idempotency, approvals, retries, and receipts.';
const canonical = '/products/crud/primer/';
---
<Base title={title} description={description} canonical={canonical}>
    <section class="container" style="padding:48px 0 8px">
        <h1>Idempotent Workflows</h1>
        <p style="color:#9ba3b7;max-width:860px">Deduplicate by <code>event_id</code>, orchestrate approvals, and execute with retries and SLOs. Emit signed receipts for audit.</p>
    </section>
</Base>


