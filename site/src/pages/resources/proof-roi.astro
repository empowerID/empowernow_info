---
import Base from '../../layouts/Base.astro';
import ArticleJsonLd from '../../components/ArticleJsonLd.astro';
const title = 'Proof & ROI — Receipts you can audit, ROI you can defend';
const description = 'Show cryptographic receipts for every action and frame measurable ROI with an evidence-first calculator.';
const canonical = '/resources/proof-roi/';
const docsBase = 'https://empowerid.github.io/empowernow_docs';
---
<Base title={title} description={description} canonical={canonical}>
    <ArticleJsonLd type="Article" title={title} description={description} url={new URL(canonical, Astro.site).toString()} />
    <section class="container" style="padding:96px 0 24px">
        <h1 class="display" style="margin:0 0 8px">Proof & ROI</h1>
        <p style="color:#9ba3b7;max-width:860px">Auditable receipts on every decision and call. A pragmatic ROI model you can defend with real usage and avoided costs.</p>
        <div style="display:flex;gap:12px;flex-wrap:wrap;margin-top:12px">
            <a class="btn btn-primary" data-ev="cta" data-label="proof-demo" data-position="hero" href={`${import.meta.env.BASE_URL}resources/demo/`}>Watch demo</a>
            <a class="btn" data-ev="cta" data-label="proof-quickstart" data-position="hero" href={`${import.meta.env.BASE_URL}docs/`}>Open Quickstart</a>
        </div>
    </section>
    <section class="container" style="padding:8px 0 24px">
        <div class="grid-3">
            <div class="glass-card" style="padding:20px">
                <h2 class="section-title" style="margin:0 0 8px">Receipts (audit)</h2>
                <p style="color:#9ba3b7;margin:0 0 12px">Every call emits a signed, hash‑chained receipt with decision_id, policy snapshot, and schema/pin hashes for forensic joins.</p>
                <ul style="color:#9ba3b7;line-height:1.8;margin:0 0 12px">
                    <li>Gate → Decide → Enforce → Prove linkage</li>
                    <li>Budget 402 over‑cap with clear reason</li>
                    <li>SIEM export and analytics joins</li>
                </ul>
                <a class="card-cta" href={`${docsBase}/docs/services/receipts/`} target="_blank" rel="noopener">Read receipts docs ↗</a>
            </div>
            <div class="glass-card" style="padding:20px">
                <h2 class="section-title" style="margin:0 0 8px">ROI model</h2>
                <p style="color:#9ba3b7;margin:0 0 12px">Per‑product levers for time‑to‑value, spend avoided, audit time saved, and incident prevention; ties to receipts and usage.</p>
                <ul style="color:#9ba3b7;line-height:1.8;margin:0 0 12px">
                    <li>Studios levers (endpoints, runs, DUs, MAUs)</li>
                    <li>Cost baselines and sensitivity ranges</li>
                    <li>Export for CFO review</li>
                </ul>
                <a class="card-cta" href={`${docsBase}/docs/website_copy/primers/primer_roi_calculator`} target="_blank" rel="noopener">Primer: ROI Calculator ↗</a>
            </div>
            <div class="glass-card" style="padding:20px">
                <h2 class="section-title" style="margin:0 0 8px">Next steps</h2>
                <ol class="numbered" style="margin:0">
                    <li>Enable receipts and export a 1‑day sample</li>
                    <li>Run the ROI model with your inputs</li>
                    <li>Align with finance; plan rollout by endpoints</li>
                </ol>
                <div style="display:flex;gap:12px;flex-wrap:wrap;margin-top:12px">
                    <a class="btn btn-primary" href={`${import.meta.env.BASE_URL}demo/`} data-ev="cta" data-label="proof-book-demo">Book demo</a>
                    <a class="btn" href={`${import.meta.env.BASE_URL}products/receipts/`}>Receipt Vault</a>
                </div>
            </div>
        </div>
    </section>
</Base>


