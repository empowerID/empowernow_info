---
import Base from '../../layouts/Base.astro';
import MediaVideo from '../../components/MediaVideo.astro';
const title = '90-Second Demo — EmpowerNow';
const description = 'Watch how EmpowerNow helps you create tools once and enforce policy everywhere with budget controls and cryptographic receipts.';
const canonical = '/resources/demo/';
const base = import.meta.env.BASE_URL;
---
<Base title={title} description={description} canonical={canonical}>
	<header class="hero-plate page-stage" style={`padding:160px 0 64px;background-image:url(${import.meta.env.BASE_URL}images/backgrounds/bg-homepage-hero.jpg);background-size:cover;background-position:center;background-repeat:no-repeat`}>
		<div class="container" style="position:relative;z-index:1">
			<div class="eyebrow" style="font-size:14px;font-weight:700;letter-spacing:0.08em;text-transform:uppercase;color:rgba(0,231,246,0.85);margin:0 0 16px">DEMO VIDEO</div>
			<h1 class="display" style={`margin:0 0 16px;font-size:clamp(2.25rem,4.5vw,3.6rem)`}>90-Second Demo</h1>
			<p style={`color:#9ba3b7;max-width:90ch;font-size:clamp(1.0125rem,1.8vw,1.2375rem)`}>See how to create agent-safe tools in minutes with budget controls and cryptographic receipts.</p>
		</div>
	</header>

	<section class="container" style="padding:48px 0 24px">
		<div class="panel-glow" style="padding:20px">
			<h2 class="section-title" style="margin:0 0 16px">The AI Gold Rush</h2>
			<MediaVideo 
				src={{ mp4: `${base}videos/homepage/The_AI_Gold_Rush.mp4` }} 
				aspect="16/9" 
				title="EmpowerNow 90-second demo: Create tools once, enforce policy everywhere" 
				priority 
			/>
		</div>
	</section>

	<section class="container" style="padding:24px 0 64px">
		<h2 class="section-title">What you'll see</h2>
		<div class="glass-card" style="padding:20px">
			<ul style="color:#9ba3b7;line-height:1.8;margin:0;padding-left:18px">
				<li><strong>Create tools in minutes</strong>: Turn any API/DB into agent-safe tools with schema pins and policy hooks</li>
				<li><strong>Publish once, run everywhere</strong>: Deploy to MCP, Microsoft Copilot, and OpenAI Functions from a single definition</li>
				<li><strong>Budget & content controls</strong>: Enforce spend limits (HTTP 402) and content filters at runtime</li>
				<li><strong>Cryptographic receipts</strong>: Provable evidence of every action with policy snapshots and cost attribution</li>
			</ul>
		</div>
	</section>

	<section class="container" style="padding:24px 0 64px">
		<h2 class="section-title">Ready to try it?</h2>
		<div class="panel-glass" style="padding:20px">
			<p style="color:#9ba3b7;margin:0 0 16px">Get started with our quickstart guide or book a personalized demo with our team.</p>
			<div style="display:flex;gap:12px;flex-wrap:wrap">
				<a class="btn btn-primary" href="https://empowerid.github.io/empowernow_docs/docs/services/crud-service/how-to/mcp-quickstart/" data-ev="cta_click" data-label="demo-page-quickstart">Start Quickstart</a>
				<a class="btn btn-ghost" href={`${base}demo/`} data-ev="cta_click" data-label="demo-page-book-demo">Book 15-min demo</a>
				<a class="btn btn-ghost" href={`${base}products/`} data-ev="cta_click" data-label="demo-page-products">Explore products</a>
			</div>
		</div>
	</section>

	<section class="container" style="padding:24px 0 64px">
		<h2 class="section-title">More resources</h2>
		<div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:16px">
			<a href={`${base}solutions/`} class="glass-card hover-rise" style="padding:16px;text-decoration:none">
				<h3 style="margin:0 0 8px;color:#e8ecff">Solutions</h3>
				<p style="color:#9ba3b7;margin:0;font-size:14px">Explore how EmpowerNow solves real-world challenges</p>
			</a>
			<a href={`${base}resources/standards/`} class="glass-card hover-rise" style="padding:16px;text-decoration:none">
				<h3 style="margin:0 0 8px;color:#e8ecff">Standards</h3>
				<p style="color:#9ba3b7;margin:0;font-size:14px">Learn about OAuth, OIDC, AuthZEN, MCP, and more</p>
			</a>
			<a href={`${base}resources/strategy/`} class="glass-card hover-rise" style="padding:16px;text-decoration:none">
				<h3 style="margin:0 0 8px;color:#e8ecff">Strategy</h3>
				<p style="color:#9ba3b7;margin:0;font-size:14px">Deep dives into authorization, architecture, and more</p>
			</a>
		</div>
	</section>
</Base>

