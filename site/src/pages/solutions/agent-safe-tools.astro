---
import Base from '../../layouts/Base.astro';
import Solution from '../../layouts/Solution.astro';
const title = 'Agent‑Safe Tools — Enforce policy at the tool boundary';
const description = 'Use MCP Gateway + ARIA Shield to verify agents, enforce constraints and obligations, and produce receipts for every tool call.';
const canonical = '/solutions/agent-safe-tools/';
---
<Base title={title} description={description} canonical={canonical}>
    <Solution title={title} description={description} canonical={canonical} headline="Agent‑Safe Tools" outcomes={[ 'Verified agents', 'Constrained tools', 'Auditable calls' ]} primaryCTA={{ label: 'Open Quickstart', href: '/docs/' }} secondaryCTA={{ label: 'Watch 90‑sec demo', href: '/resources/demo/' }} docsLinks={[ { href: 'https://empowerid.github.io/empowernow_docs/docs/services/mcp-gateway/', label: 'Gateway Docs' } ]} wwwLinks={[ { href: '/products/gateway/', label: 'MCP Gateway' }, { href: '/products/aria-shield/', label: 'ARIA Shield' } ]} keywords={[ 'mcp', 'authzen' ]}>
		<fragment slot="how">
			<section class="container" style="padding:8px 0 24px">
				<h2 class="section-title">How it works</h2>
				<ol class="numbered">
					<li>Agent presents ARIA Passport to Gateway.</li>
					<li>Gateway validates and checks policy for the requested tool.</li>
					<li>Constraints and obligations enforced; tool is invoked.</li>
					<li>Receipt is created with full context.</li>
				</ol>
			</section>
      <section class="container" style="padding:8px 0 24px">
        <h2 class="section-title">FAQ</h2>
        <ul style="color:#9ba3b7;line-height:1.8">
          <li><strong>How do schema pins work?</strong> Gateway validates tool/model schema hash/version and blocks drift pre‑exec.</li>
          <li><strong>Can we restrict tools per agent?</strong> Yes. AuthZEN constraints and allow‑lists per agent/role.</li>
          <li><strong>Do we get receipts?</strong> Every governed call emits a signed receipt for audit and chargeback.</li>
        </ul>
      </section>
      <section class="container" style="padding:8px 0 24px">
        <h2 class="section-title">Watch: Tool Governance in 90 seconds</h2>
        <div class="glass-card" style="padding:12px">
          <div style="position:relative;padding-top:56.25%">
            <iframe src="https://www.youtube.com/embed/dQw4w9WgXcQ" title="Tool Governance demo" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen style="position:absolute;top:0;left:0;width:100%;height:100%;border:0"></iframe>
          </div>
        </div>
      </section>
		</fragment>
        <fragment slot="standards">
            <section class="container" style="padding:8px 0 24px">
                <h2 class="section-title">Standards</h2>
                <ul style="color:#9ba3b7;line-height:1.8">
                    <li>OpenID AuthZEN obligations/constraints</li>
                    <li>RAR (RFC 9396), DPoP (RFC 9449)</li>
                    <li>MCP model/tool schema pins</li>
                </ul>
            </section>
        </fragment>
	</Solution>
</Base>
