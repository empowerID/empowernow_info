---
import Base from '../../layouts/Base.astro';
const title = 'Solutions — EmpowerNow';
const description = 'Create tools once. Enforce policy and budgets across any agent platform. Prove every action with receipts.';
const canonical = '/solutions/';
const items = [
	{ href: '/solutions/zero-token-spas/', title: 'Zero‑Token SPAs', desc: 'Session UX with BFF + AuthZEN decisions' },
	{ href: '/solutions/agent-safe-tools/', title: 'Agent‑Safe Tools', desc: 'MCP Gateway + ARIA Shield' },
	{ href: '/solutions/spend-governance/', title: 'Spend Governance', desc: 'Stream caps + Receipts + Analytics' },
	{ href: '/solutions/delegated-authorization/', title: 'Delegated Authorization', desc: 'AuthZEN PDP + Membership' },
	{ href: '/solutions/hybrid-connectivity/', title: 'Hybrid Connectivity', desc: 'Orchestration + Registry + PDP' },
];
---
<Base title={title} description={description} canonical={canonical}>
    <section class="container" style="padding:48px 0 8px">
		<h1>Solutions</h1>
		<p style="color:#9ba3b7;max-width:880px;margin:6px 0 16px">Create agent tools once, <strong>enforce</strong> policy and budgets across any agent platform, and <strong>prove</strong> every action with receipts.</p>
        <p style="color:#9ba3b7;max-width:900px;margin:0 0 8px">
			Flow: <strong><a href={`${import.meta.env.BASE_URL}products/crud/`}>Design</a></strong> (Automation/CRUDService) →
			<strong><a href={`${import.meta.env.BASE_URL}products/pdp/`}>Decide</a></strong> (Authorization/PDP, OpenID AuthZEN) →
			<strong><a href={`${import.meta.env.BASE_URL}products/gateway/`}>Enforce</a></strong> (MCP Gateway/<a href={`${import.meta.env.BASE_URL}products/aria-shield/`}>ARIA Shield</a>) →
			<strong><a href={`${import.meta.env.BASE_URL}products/receipts/`}>Prove</a></strong> (Receipt Vault/<a href={`${import.meta.env.BASE_URL}products/data-collector/`}>Data Collector</a>)
        </p>
	</section>
	<section class="container" style="padding:8px 0 24px">
		<h2 class="section-title">Proof (last 30 days)</h2>
		<div class="glass-card" style="padding:12px;display:flex;gap:16px;flex-wrap:wrap;color:#9ba3b7">
			<div><strong>Receipts/day</strong> — __k</div>
			<div><strong>Spend blocked (402)</strong> — __%</div>
			<div><strong>Governed endpoints</strong> — __</div>
		</div>
	</section>
	<section class="container" style="padding:8px 0 24px">
		<h2 class="section-title">Build & Publish</h2>
		<div class="grid-3">
            <a href={`${import.meta.env.BASE_URL}products/crud/`} class="glass-card" style="padding:16px">
                <h3 style="margin:0 0 8px">No‑Code Connectors (<abbr title="Anthropic's Model Context Protocol">MCP</abbr> Tools)</h3>
                <p style="color:#9ba3b7;margin:0 8px 10px 0;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden">Turn any API/DB into agent‑safe tools in minutes, safely — schema‑pinned, auth‑aware, policy‑ready.</p>
                <div style="display:flex;gap:8px;flex-wrap:wrap"><span class="chip chip--outline">AI Platform</span><span class="chip chip--outline">Integrations</span><a class="btn btn-primary" href={`${import.meta.env.BASE_URL}products/crud/`}>Quickstart</a><a class="btn" href={`${import.meta.env.BASE_URL}resources/`}>Adapter catalog</a></div>
			</a>
			<a href={`${import.meta.env.BASE_URL}products/crud/`} class="glass-card" style="padding:16px">
				<h3 style="margin:0 0 8px">No‑Code Workflows for Agents</h3>
                <p style="color:#9ba3b7;margin:0 8px 10px 0;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden">Orchestrate multi‑step agent actions with approvals, retries, and obligations — without code.</p>
                <div style="display:flex;gap:8px;flex-wrap:wrap"><span class="chip chip--outline">AI Platform</span><span class="chip chip--outline">Ops</span><a class="btn btn-primary" href={`${import.meta.env.BASE_URL}products/crud/`}>Workflow Quickstart</a><a class="btn" href={`${import.meta.env.BASE_URL}products/crud/`}>Examples</a></div>
			</a>
		</div>
	</section>
	<section class="container" style="padding:8px 0 24px">
		<h2 class="section-title">Secure Execution & Spend Control</h2>
        <div class="grid-3">
            <div class="glass-card" style="padding:16px">
                <h3 style="margin:0 0 8px">Agent‑Safe Tools</h3>
                <p style="color:#9ba3b7;margin:0 0 10px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden">Block prompt‑injection and tool overreach before execution; enforce budgets, params, and egress at runtime.</p>
                <div style="display:flex;gap:8px;flex-wrap:wrap"><span class="chip chip--outline">AI Platform</span><span class="chip chip--outline">AppSec</span><a class="btn btn-primary" href={`${import.meta.env.BASE_URL}products/gateway/`}>Try Gateway</a><a class="btn" href={`${import.meta.env.BASE_URL}resources/proof-roi/`}>View a receipt</a></div>
            </div>
            <div class="glass-card" style="padding:16px">
                <h3 style="margin:0 0 8px">Spend Governance</h3>
                <p style="color:#9ba3b7;margin:0 0 10px;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden">Cap per‑call/agent spend; attribute every dollar to policy, user, and tool <strong>with receipts</strong>.</p>
                <div style="display:flex;gap:8px;flex-wrap:wrap"><span class="chip chip--outline">FinOps</span><span class="chip chip--outline">CISO</span><a class="btn btn-primary" href={`${import.meta.env.BASE_URL}resources/proof-roi/`}>FinOps guide</a><a class="btn" href={`${import.meta.env.BASE_URL}resources/proof-roi/`}>Receipts schema</a></div>
            </div>
        </div>
	</section>
	<section class="container" style="padding:8px 0 24px">
		<h2 class="section-title">Identity & Access for Agents</h2>
		<div class="grid-3">
			{[items[3], items[0], items[4]].map(p => (
				<a href={`${import.meta.env.BASE_URL}${p.href.replace(/^\//,'')}`} class="glass-card" style="padding:16px">
					<h3 style="margin:0 0 8px">{p.title}</h3>
					<p style="color:#9ba3b7;margin:0">{p.desc}</p>
				</a>
			))}
		</div>
	</section>
	<section class="container" style="padding:8px 0 24px">
		<h2 class="section-title">Works with</h2>
		<div style="display:flex;gap:8px;flex-wrap:wrap;color:#9ba3b7">
			<span class="chip">Anthropic MCP</span><span class="chip">Microsoft Copilot</span><span class="chip">OpenAI Functions</span><span class="chip">AWS Bedrock</span><span class="chip">Google Vertex</span><span class="chip">LangChain/LangGraph</span>
		</div>
	</section>
    <style>
        /* Unify link colors; remove default visited purple */
        .container a:link, .container a:visited { color: var(--color-primary); text-decoration: none; }
        .container a:hover { text-decoration: underline; }
        /* Make chips quieter than CTAs */
        .chip { font-size: .85rem }
        .btn.btn-primary { box-shadow: 0 6px 24px rgba(0,231,246,.25) }
    </style>

</Base>
